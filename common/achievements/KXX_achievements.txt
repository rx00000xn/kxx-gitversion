unique_id = kxxflavourachievements_1936

################################################################
# The Achievements, most of them
################################################################


FRA_hidden_path = {
    possible = {
        # classic triggers used in all vanilla achievements
        has_start_date < 1936.01.02
		#achievment specific
        original_tag = FRA		
    }
    happened = {
        original_tag = FRA
        custom_trigger_tooltip = {
			tooltip = KXX_hidden_achievement_tt
			has_country_flag = FRA_mod_jac_sup
		}
    }
}
WHR_hidden_path = {
    possible = {
        # classic triggers used in all vanilla achievements
        has_start_date < 1936.01.02
		#achievment specific
        original_tag = WHR		
    }
    happened = {
        original_tag = WHR
        custom_trigger_tooltip = {
			tooltip = KXX_hidden_achievement_tt
			has_country_flag = WHR_hidden_path_unlock
		}
    }
}
DEN_hidden_path = {
    possible = {
        # classic triggers used in all vanilla achievements
        has_start_date < 1936.01.02
		#achievment specific
        original_tag = DEN	
    }
    happened = {
        original_tag = DEN
        custom_trigger_tooltip = {
			tooltip = KXX_hidden_achievement_tt
			has_country_flag = DEN_hidden_path_unlock
		}
    }
}
QIE_hidden_path = {
    possible = {
        # classic triggers used in all vanilla achievements
        has_start_date < 1936.01.02
		#achievment specific
        original_tag = QIE
    }
    happened = {
       original_tag = QIE
        custom_trigger_tooltip = {
			tooltip = KXX_hidden_achievement_tt
			has_country_flag = QIE_hidden_path_unlock
		}
       
    }
}
GRE_hidden_path = {
    possible = {
        # classic triggers used in all vanilla achievements
        has_start_date < 1936.01.02
		#achievment specific
        original_tag = GRE
    }
    happened = {
        original_tag = GRE
        custom_trigger_tooltip = {
			tooltip = KXX_hidden_achievement_tt
			has_country_flag = GRE_hidden_path_unlock
		}
    }
}
SER_hidden_path = {
    possible = {
        # classic triggers used in all vanilla achievements
        has_start_date < 1936.01.02
		#achievment specific
        original_tag = SER
    }
    happened = {
        original_tag = SER
        custom_trigger_tooltip = {
			tooltip = KXX_hidden_achievement_tt
			has_country_flag = SER_hidden_path_unlock
		}
    }
}
CHI_hidden_path = {
    possible = {
        # classic triggers used in all vanilla achievements
        has_start_date < 1936.01.02
		#achievment specific
        #original_tag = LEP
    }
    happened = {
        original_tag = CHI #has to be here because you can't start as lkmt
        custom_trigger_tooltip = {
			tooltip = KXX_hidden_achievement_tt
			has_country_flag = CHI_hidden_path_unlock
		}
    }
}
SQI_hidden_path = {
    possible = {
        # classic triggers used in all vanilla achievements
        has_start_date < 1936.01.02
		#achievment specific
        original_tag = SQI
    }
    happened = {
        original_tag = SQI
        custom_trigger_tooltip = {
			tooltip = KXX_hidden_achievement_tt
			has_country_flag = SQI_hidden_path_unlock
		}
    }
}