add_namespace = KXXAI_improve

country_event = {
	id = KXXAI_improve.1
	immediate = { 
        log = "[GetDateText]: [Root.GetName]: event KXXAI_improve.1" 
        set_country_flag = KXXAI_GRE_GD
    }
	title = KXXAI_improve.1.t
	desc = KXXAI_improve.1.d
	picture = GFX_report_event_GRE_crowd2

	trigger = {
		tag = GRE       
        is_ai = yes
		is_subject = no
        date > 1938.1.1
        NOT = {
            has_socialist_government = yes
        }
        NOT = {
            has_country_flag = KXXAI_GRE_GD
        }        
        OR = {
            RUS = { has_government = national_populist }
            SER = { has_government = national_populist }
            BUL = { has_government = national_populist }
            TUR = { has_government = national_populist }
            CAN = { has_government = national_populist }
            NFA = { has_government = national_populist }
            AUS = { has_government = national_populist }
        }
	}

    fire_only_once = yes

	option = {
		name = KXXAI_improve.1.a
        ai_chance = { base = 80 } 
        country_event = gre.415
	}

	option = {
		name = KXXAI_improve.1.b
        ai_chance = { base = 20 }
	}

}